name: logines
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'  # Ajustado para soportar null safety

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.6  # Versión compatible con null safety

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/  # Agrega esta línea para incluir la carpeta assets
    - assets/app_icon.png